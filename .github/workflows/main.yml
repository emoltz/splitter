#name: Deploy to AWS Elastic Beanstalk
#
#on:
#  push:
#    branches:
#      - main  # or your default branch
#
#jobs:
#  build-and-deploy:
#    runs-on: ubuntu-latest
#
#    steps:
#      - name: Checkout
#        uses: actions/checkout@v2
#
#      - name: Set up JDK 11
#        uses: actions/setup-java@v2
#        with:
#          java-version: '11'
#          distribution: 'adopt'
#
#      - name: Build with Maven
#        run: mvn clean package -DskipTests
#
#      - name: Deploy to AWS Elastic Beanstalk
#        uses: einaregilsson/beanstalk-deploy@v13
#        with:
#          aws_access_key: ${{ secrets.AWS_ACCESS_KEY_ID }}
#          aws_secret_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#          application_name: backend # replace with your EB application name
#          environment_name: Backend-env  # replace with your EB environment name
#          region: ${{ secrets.AWS_REGION }}
#          version_label: github-${{ github.run_id }}
#          deployment_package: target/your-app.jar  # replace with your jar path
